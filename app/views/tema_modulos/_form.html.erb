<%= form_with(model: tema_modulo, local: true) do |form| %>
  <% if tema_modulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tema_modulo.errors.count, "error") %> prohibited this tema_modulo from being saved:</h2>

      <ul>
      <% tema_modulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tema %>
    <%= form.text_area :tema %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
