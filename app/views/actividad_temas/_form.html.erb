<%= form_with(model: actividad_tema, local: true) do |form| %>
  <% if actividad_tema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actividad_tema.errors.count, "error") %> prohibited this actividad_tema from being saved:</h2>

      <ul>
      <% actividad_tema.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
